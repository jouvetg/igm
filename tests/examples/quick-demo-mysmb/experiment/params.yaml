# @package _global_

hydra:
  job:
    chdir: True

defaults:
  - /user/conf/modules@modules.mysmb: mysmb
  
  - override /input: [oggm_shop]
  - override /modules: [mysmb, iceflow, time, thk, vert_flow, particles]
  - override /output: [write_ncdf,plot2d]

input:
  oggm_shop:
    RGI_ID: "RGI60-11.01450"
    RGI_version: 6

modules:
  time:
    start: 2000.0
    end: 2100.0
    save: 10.0

output:
  plot2d:
    live: true
