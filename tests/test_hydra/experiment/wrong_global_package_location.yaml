# comment causing this to fail

# @package _global_ -> EVEN IF A COMMENT IS ABOVE, THE OVERRIDES WILL BE MESSED UP !

core:
  logging: True
  logging_level: 10
  logging_file: "logging file directory"
  print_params: True
  gpu_info: True
  gpu_id: 2
  saved_params_filename: params_saved
  url_data: ""
  folder_data: data_folder
